source 'https://cdn.cocoapods.org'
source 'https://github.com/grigorye/podspecs.git'

install! 'cocoapods', :integrate_targets => false

platform :ios, '10.0'
pod 'GEContinuousIntegration', '~> 0.1'

target 'Dummy' do # Make `pod install` possible.
end
